<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% names.forEach(function(people) { %>
        <% if (id == people.id) { %>
            <h1> <%= people.name %> is at this URL</h1>
        <% } %>
    <% }) %>
</body>
</html>